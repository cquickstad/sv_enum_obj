# sv_enum_obj
Object-based enumerations for SystemVerilog: singletons plus a randomizable holder. Attach methods to values, add or override encodings without editing the original type, and keep package-local forks. Optional UVM factory support. Not a drop-in for native enums—use it when you need polymorphism and extension.
